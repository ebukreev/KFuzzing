
fun bar ( x :  #!
@file : open typealias open = ( ( ) -> infix ) Int ) : Int =  #!
@file : open typealias open = ( ( ) -> infix ) #!
object lateinit x + 1
fun baz ( b : Boolean ) :  #!
@file : open typealias open = ( ( ) -> infix ) #!
object lateinit  package finally import init object expect Boolean =  #!
@file : open typealias open = ( ( ) -> infix ) #!
object lateinit  package finally import init object expect #!
fun const ( ) ! b

fun foo ( ) {
    val x : Int ?  = null

    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )
    if ( x != null ) bar ( - x )
    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )

    val b : Boolean ?  = null
    baz ( < ! INAPPLICABLE_CANDIDATE ! > ! < ! > b )
    if ( b != null ) baz ( ! b )
}