
fun bar ( x : Int ) : Int = x + 1
fun baz ( b : Boolean ) : Boolean =   @file : data @file : tailrec typealias tailrec = ( ) -> ( ( dynamic ? ) ) interface open ! b

@file : data @file : tailrec typealias tailrec = ( ) -> ( ( dynamic ? ) ) interface open @file : [ init ( 0 ) protected ( when { } ) external ( ) ] package constructor import suspend import out val finally fun foo ( ) {
    val x : Int ?  = null

    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )
    if ( x != null ) bar ( - x )
    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )

    val b : Boolean ?  = null
    baz ( < ! INAPPLICABLE_CANDIDATE !   @file : data @file : tailrec typealias tailrec = ( ) -> ( ( dynamic ? ) ) interface open @file : [ init ( 0 ) protected ( when { } ) external ( ) ] package constructor import suspend import out val finally @file : set package inner typealias import = catch interface infix > ! < ! > b )
    if ( b != null ) baz ( ! b )
}
