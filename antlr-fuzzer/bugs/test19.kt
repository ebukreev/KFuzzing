
fun bar ( x : Int ) : Int =  #!


package public x + 1  #!


package public package reified import actual object constructor val catch
fun baz ( b : Boolean ) : Boolean =  #!


package public package reified import actual object constructor val catch

@file : sealed typealias sealed = out ! b

fun foo ( ) {
    val x : Int ?  = null

    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )
    if ( x != null ) bar ( - x )
    bar ( < ! INAPPLICABLE_CANDIDATE ! > - < ! > x )

    val b : Boolean ?  = null
    baz ( < ! INAPPLICABLE_CANDIDATE ! > ! < ! > b )
    if ( b != null ) baz ( ! b )
}
